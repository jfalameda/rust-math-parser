pub struct Interpreter {
    ast: Option<Box<Node>>
}

impl Interpreter {
    pub fn new(ast: Option<Box<Node>>) -> Self {
        Interpreter {
            ast
        }
    }

    pub fn run_program() {
        
    }
}